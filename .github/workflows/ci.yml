name: Run JMeter Tests

on:
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  run-jmeter-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up JMeter
        run: |
          wget https://dlcdn.apache.org//jmeter/binaries/apache-jmeter-5.6.3.tgz
          tar -xvzf apache-jmeter-5.6.3.tgz
          sudo mv apache-jmeter-5.6.3 /opt/jmeter
          sudo ln -s /opt/jmeter/bin/jmeter /usr/local/bin/jmeter

      - name: Run JMeter Tests
        run: |
          jmeter -n -t jmeter-tests/Test\ Plan\ South.jmx -l results/results.jtl
          ls -l results

      - name: Generate Report
        run: |
          jmeter -g results/results.jtl -o results/jmeter-report

      - name: Compress Report into ZIP
        run: |
          zip -r jmeter-report.zip results/jmeter-report

      - name: Upload ZIP Artifact
        uses: actions/upload-artifact@v3
        with:
          name: JMeter Report ZIP
          path: jmeter-report.zip